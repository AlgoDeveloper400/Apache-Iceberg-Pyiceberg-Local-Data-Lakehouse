# Core data lakehouse dependencies
pyiceberg[sql-sqlite]>=0.6.0
pyarrow>=14.0.0

# Optional: install extras for cloud backends if needed
# pyiceberg[glue]       — AWS Glue catalog
# pyiceberg[hive]       — Hive metastore catalog
# pyiceberg[s3fs]       — S3 storage